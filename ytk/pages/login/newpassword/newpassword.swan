<view>
	<view class="warpper">
		<view>为账号设置新密码</view>
		<input class="tel" bindinput='password' placeholder="设置6-16位密码" />
		<button class="btn" class="btn {{length >=6 ? 'ready': ''}}" bindtap="comit"
			disabled="{{length >= 6 ? false: true}}">完成</button>
	</view>
</view>